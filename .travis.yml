language: python
dist: focal
python:
  - "3.8"

before_script:
  - echo "This runs before the main script."

install:
  - python --version
  - pip --version || (sudo apt-get update -y && sudo apt-get install -y python3-pip)
  - if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

script:
  - echo "Running a simple test..."
  - python -c "print('Hello, Travis!')"

after_script:
  - echo "This runs after the main script."
